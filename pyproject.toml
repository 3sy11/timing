[project]
name = "timing"
version = "0.1.0"
description = "交易系统首期：斐波那契回撤 + 触线，基于 bollydog"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2",
    "bollydog",
    "duckdb",
]

[tool.uv.sources]
bollydog = { path = "../bollydog", editable = true }
