# 附录 A0：场景故事

场景故事是系统设计的起点。每条故事用**一句话**描述用户视角的需求，不涉及任何类名、技术术语或实现机制。

后续步骤中，每条被选定的故事会对应一张精确的顺序图（步骤②），逼出所有接口和方法签名。

---

## A 类：生产模式（实时推送）

系统启动后常驻运行，等待外部推送数据，实时处理并响应。

| ID | 场景故事 |
|----|---------|
| A1 | 推一根新K线，系统检测到价格靠近关键位，产出交易信号，策略判定信号有效后下单买入并记账 |
| A2 | 推一根新K线，系统检测后未触碰任何关键位，不产出信号，链路静默结束 |
| A3 | 推一根新K线，价格突破了指标的有效边界，系统重新计算指标结构并保存更新后的数据 |
| A4 | 推一根新K线，系统产出交易信号，但策略判定信号强度不够，放弃下单并记录跳过原因 |
| A5 | 系统产出信号且策略通过，但账户余额不足以下单，记录拒绝并广播拒绝事件 |
| A6 | 查看系统中所有的中间记录：哪些信号被产出、哪些被策略接受或拒绝、哪些订单成交了 |
| A7 | 策略下了一个限价单，当时没成交，后续某根K线价格触及目标价后自动成交并记账 |
| A8 | 系统启动后加载配置，初始化所有服务，等待外部推送数据 |
| A9 | 查看当前持仓和账户余额 |
| A10 | 通过命令行将数据文件中的K线导入到系统数据库中 |

---

## B 类：回测模式（命令行启动）

命令行一次性启动，读取历史数据，完整回放后输出结果并退出。

| ID | 场景故事 |
|----|---------|
| B1 | 命令行启动回测，系统读取历史K线，分析处理后产出一组交易信号，策略逐个判断后下单，执行器完成所有交易并记账 |
| B2 | 回测结束后，查看所有中间结果：每根K线产出了什么信号、策略做了什么决策、每笔订单的成交价和手续费 |
| B3 | 回测结束后，查看最终账户盈亏、总手续费、成交笔数等汇总指标 |
| B4 | 两次回测使用独立的存储空间，互不干扰，可以对比两次结果 |
| B5 | 回测中某些信号被策略跳过，某些订单被余额拒绝，最终只有部分信号变成了成交 |
| B6 | 回测中包含限价单，在历史数据的后续K线中被触发成交 |

---

## C 类：参数实验（批量回测 + 可视化）

对指标参数的合理性存疑，需要通过批量实验和可视化来验证。

| ID | 场景故事 |
|----|---------|
| C1 | 不确定指标参数是否合理，对单次回测的中间结果（信号列表、指标状态）查表画图，判断指标是否有效 |
| C2 | 配置多组不同的指标参数，系统自动对每组参数分别执行完整回测 |
| C3 | 批量回测结束后，一次性查看所有实验的中间过程和最终结果，用图表对比不同参数下的表现差异 |
| C4 | 在图表上叠加显示K线走势、指标关键位、信号触发点、买卖成交点，一眼看清策略在每个位置做了什么 |
| C5 | 对比两组参数的收益曲线，看参数敏感度——参数小幅变化是否导致收益大幅波动 |
| C6 | 导出某次实验的完整中间数据，用外部工具做进一步分析 |

---

## 故事之外的实现细节

以下细节不属于场景故事（用户不关心"怎么做"），但在后续步骤中需要体现：

| 细节 | 归属步骤 | 说明 |
|------|---------|------|
| 分析服务的 checkpoint 增量处理 | ②顺序图 | 分析服务收到"数据已更新"通知后，不是回放所有K线，而是按自己的 checkpoint 只获取最新的增量数据，逐条处理后更新 checkpoint。故事中只说"系统处理新数据产出信号"，不涉及增量机制 |
| 指标突破后的全量重算 | ②顺序图 | A3 中"重新计算指标"的触发条件（突破边界）和重算范围（拉全量K线重新计算）是内部流程细节 |
| 生产模式异步 vs 回测模式同步 | ②顺序图 | 故事不区分异步/同步，但顺序图中两种模式的事件传递方式不同 |
| 回测中"逐根处理"的执行者 | ②顺序图 | B1 中"分析处理"由谁发起、每根K线是外层循环推入还是分析服务内部拉取，是顺序图要明确的内部分工 |
| 存储路径隔离 | ②顺序图 | B4 中"独立存储空间"的具体路径规则是实现细节 |
| 各层中间结果的序列化时机 | ②顺序图 | A6/B2 中"查看中间记录"依赖每一层在什么时机把结果写入存储，这是顺序图中的具体调用 |
