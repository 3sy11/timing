# fib002 — 中短期均衡：介于 fib001(短期敏感) 和 fib003(长期稳健) 之间
pivot_windows = [[5, 5], [8, 8]]
zigzag_thresholds = [0.05, 0.10]
regression_windows = [50, 100]
recent_bars = 60
skip_recent = 10
min_leg_span_pct = 0.03
top_n = 4
cluster_tolerance_pct = 0.004
min_cluster_conf = 0.22
min_bars = 200
# Fib 拟合质量门槛
min_fit_score = 1.5
# 失效
invalidate_break_bars = 5
boundary_tolerance_k = 0.02
vacancy_retry_interval = 5
# stale/coverage 失效
stale_lookback = 25
stale_touch_tol_k = 0.05
stale_min_coverage = 0.4
stale_check_interval = 10
