# 回测配置 B — 不同品种的回测
# run_id = "exp_large_cap"

[[services]]
module = "timing.analysis.algo.retracement.service.RetracementService"
symbol = "510300.OF"
interval = "1d"
warmup_bars = 200
[services.config]
touch_tolerance = 0.3
min_leg_span_pct = 0.04

[[services]]
module = "timing.analysis.algo.retracement.service.RetracementService"
symbol = "513090.OF"
interval = "1d"
warmup_bars = 200
[services.config]
touch_tolerance = 0.25
min_leg_span_pct = 0.03
